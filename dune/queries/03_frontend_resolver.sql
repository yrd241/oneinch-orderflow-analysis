-- Q3: Map tx -> frontend_label / category via router `to` + 1inch referrer calldata.
-- Join with seed table of known wallet routers (MetaMask Swaps, Rabby, …).

SELECT CAST(NULL AS VARCHAR) AS hash;
